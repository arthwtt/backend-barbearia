services:
  - type: web
    name: backend-barbearia
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: RUN_SEEDS
        value: false
